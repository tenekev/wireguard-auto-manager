name: Building Executable
run-name: ${{ github.actor }} 
on: [push]

permissions:
  contents: write

jobs:
  Run-PS2EXE-on-Windows:
    name: Run PS2EXE on Windows
    runs-on: windows-latest
    steps:
      
      - uses: actions/checkout@v3

      - shell: powershell
        run: |
          tree C:\program files\powershell\7\Modules

      - name: Setup PowerShell module cache
        uses: actions/cache@v3
        id: cacher
        with:
          path: "%USERPROFILE%\\Documents\\PowerShell\\Modules"
          key: ${{ runner.os }}-SqlServer-PSScriptAnalyzer
      
      - name: Install required PowerShell modules
        if: steps.cacher.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          Set-PSRepository PSGallery -InstallationPolicy Trusted
          Install-Module ps2exe

      - shell: powershell
        run: |
          tree C:\program files\powershell\7\Modules

      - shell: powershell
        run: |
          tree C:\Users\runneradmin\
      
      - name: Build binary
        shell: powershell
        run: |
          . ps2exe wg_auto_manager.ps1 wg_auto_manager.exe -NoConsole -noOutput

      - name: Release
        uses: softprops/action-gh-release@v2
        #if: startsWith(github.ref, 'refs/tags/')
        with:
          tag_name: "v1"
          files: "*.exe"
